#!/bin/sh
# DF_Unified/STATUS -- df_unified status (offline; NETWORK=deny)
set -eu
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PY="${PYTHON:-python3}"
exec "$PY" -B "$ROOT/df_unified/cli.py" status "$@"
