#!/bin/sh
# DF_Unified/RUN -- df_unified run (offline; NETWORK=deny)
set -eu
ROOT="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
PY="${PYTHON:-python3}"
exec "$PY" -B "$ROOT/df_unified/cli.py" run "$@"
